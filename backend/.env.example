PORT=5001
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
NODE_ENV=development
FRONTEND_URL=http://localhost:3000

# PostgreSQL Database Configuration
# Note: On macOS with Homebrew, DB_USER is usually your system username (run 'whoami' to check)
# If you get "role postgres does not exist", try using your system username instead
DB_HOST=localhost
DB_PORT=5432
DB_NAME=reservations_db
DB_USER=postgres
DB_PASSWORD=postgres